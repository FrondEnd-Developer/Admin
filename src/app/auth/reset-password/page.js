import { ClassicNewPasswordView } from 'src/sections/auth';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth : reset-password',
};

export default function ClassicNewPasswordViewPage() {
  return <ClassicNewPasswordView />;
}
