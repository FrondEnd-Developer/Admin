import { Classic } from 'src/sections/auth';
// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Classic: Verify',
};

export default function ClassicVerifyPage() {
  return <Classic />;
}
