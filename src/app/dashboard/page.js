import { RestListView } from 'src/sections/restaurant/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: View',
};

export default function Page() {
  return <RestListView />;
}
